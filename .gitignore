# ---------- Build directories ----------
build/
build-*/
out/
cmake-build-*/
build_*/
CMakeCache.txt
cmake_install.cmake
CMakeFiles/

# ---------- Qt generated artifacts (when not using AUTOMOC/AUTORCC) ----------
moc_*/
qrc_*.cpp
ui_*.h

# ---------- IDE / OS ----------
*.pro.user
*.pro.user.*
.DS_Store
Thumbs.db
.vscode/
.idea/

# ---------- Temporary OCR artifacts (runtime, never commit) ----------
*.tmp
~*.png
~*.txt
*.ocr.png
*.ocr.txt

# ---------- Tesseract dependencies (do NOT commit trained data / binaries) ----------
tessdata/
*.traineddata

# ---------- Temporary / local-only docs (not part of the shipped project) ----------
tmp/

# ---------- Local build override (machine-specific paths, NOT committed) ----------
build.local.bat
